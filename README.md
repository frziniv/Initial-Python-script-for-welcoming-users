# Initial-Python-script-for-welcoming-users
# main_script.py

def greet_user(name):
    """Prints a standard personalized welcome message."""
# main_script.py

def greet_user(name):
    """Prints a standard personalized welcome message."""
    print(f"Hello, {name}! Welcome to the project repository.")

if __name__ == "__main__":
    greet_user("GitHub User")
if __name__ == "__main__":
Imported the datetime module and updated the greet_user function to display the current server timestamp alongside the welcome message.
